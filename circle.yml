machine:
  node:
    version: 8.2.0
dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: master
    commands:
      - npm run build
      - aws s3 sync /home/ubuntu/tudr.xyz/dist s3://tudr.xyz --delete
  